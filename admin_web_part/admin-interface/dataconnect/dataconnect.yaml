specVersion: "v1beta"
serviceId: "admin-interface"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "admin-interface-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
